from django.db.models.signals import post_save
from django.dispatch import receiver
# from .models import Models

# @receiver(post_save, sender=Models)
# def trigger_action(sender, instance, **kwargs):
#     print(instance)